# act配置文件
# 使用更小的Docker镜像以加快测试速度

--platform ubuntu-latest=catthehacker/ubuntu:act-latest
--platform macos-latest=sickcodes/docker-osx:monterey
--platform windows-latest=catthehacker/ubuntu:runner-latest

# 启用容器重用以加快后续运行
--reuse

# 默认显示详细输出
--verbose

# 设置默认secrets文件位置（如果存在）
--secret-file .secrets